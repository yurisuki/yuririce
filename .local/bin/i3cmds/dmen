#!/bin/sh
# This script will open dmenu
# using wal colors if they are present.

# Firstly, you have to import the colors.
source "~/.cache/wal/colors.sh" || dmenu_run && exit

dmenu_run -nb "$color0" -nf "$color15" -sb "$color1" -sf "$color15"
